import React from "react";
import Button from "@mui/material/Button";

const AddWorkoutButton = () => {
  return <Button variant="contained">Add workout</Button>;
};

export default AddWorkoutButton;
